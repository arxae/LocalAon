# LocalAeon Models

These contain the EF database models.

Use the following convention:
- Always inherit from IPageModel
- All properties should be nullable, except for the interface methods
- Include a constant with the WebsiteCategory called WEBSITE_CATEGORY
